# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-Party and are governed by
# separate terms of service, privacy policy, and support
# documentation.
# This workflow will build a Java project with Gradle and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-gradle

name: test

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - test

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'

    # Configure Gradle for optimal use in GitHub Actions, including caching of downloaded dependencies.
    # See: https://github.com/gradle/actions/blob/main/setup-gradle/README.md
    #- name: Setup Gradle
     # uses: gradle/actions/setup-gradle@af1da67850ed9a4cedd57bfd976089dd991e2582 # v4.0.0

    - name: Make gradlew executable
      run: chmod +x ./gradlew

    - name: Build with Gradle Wrapper
      run: |
        ./gradlew patchChangelog \
        -Ptoken=${{ secrets.JETBRAINS_M_TOKEN }} \
        -PpluginVersion=1.1.8
#        -PpluginRepositoryUrl=https://github.com/Finncu/fdm-Plugin.git \
#        -PpluginGroup=de.cyan.fca \
#        -PpluginName=FDM \
#        -PpluginSinceBuild=242 \
#        -PplatformType=IC \
#        -PplatformVersion=2025.1.5 \
#        -PgradleVersion=9.0.0 \
#        -Pkotlin.stdlib.default.dependency=false \
#        -Porg.gradle.configuration-cache=true \
#        -Porg.gradle.caching=true

    # NOTE: The Gradle Wrapper is the default and recommended way to run Gradle (https://docs.gradle.org/current/userguide/gradle_wrapper.html).
    # If your project does not have the Gradle Wrapper configured, you can use the following configuration to run Gradle with a specified version.
    #
    # - name: Setup Gradle
    #   uses: gradle/actions/setup-gradle@af1da67850ed9a4cedd57bfd976089dd991e2582 # v4.0.0
    #   with:
    #     gradle-version: '8.9'
    #
    # - name: Build with Gradle 8.9
    #   run: gradle build
    - name: Commit Changelog
      #      run: git checkout -b v${{ github.ref_name }} && git add CHANGELOG.md && git commit CHANGELOG.md -m "updated Changelog by pipeline" && git push --set-upstream origin v${{ github.ref_name }}
      run: git add CHANGELOG.md && git commit CHANGELOG.md -m "updated Changelog by pipeline" || echo "nothing to do" && git push origin HEAD:main || echo "[INFO] --- Something went wrong" && git checkout -b v1.1.8 && git push --set-upstream origin v1.1.8
      env:
        GIT_AUTHOR_NAME: github-actions
        GIT_AUTHOR_EMAIL: github-actions@cuypers.dev
        GIT_COMMITTER_NAME: github-actions
        GIT_COMMITTER_EMAIL: github-actions@cuypers.dev
